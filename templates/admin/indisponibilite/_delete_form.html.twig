<form method="post" action="{{ path('app_indisponibilite_delete', {'id': indisponibilite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ indisponibilite.id) }}">
    <button class="btn">Delete</button>
</form>
