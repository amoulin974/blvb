{% extends 'base.html.twig' %}

{% block title %}Modification du lieu : {{ lieu.nom }}{% endblock %}

{% block body %}
    <h1>Modification du lieu : {{ lieu.nom }}</h1>

    <div class="container ">
    {{ include('admin/lieu/_form.html.twig', {'button_label': 'Sauvegarder'}) }}

        <div class="flex">
            <a href="{{ path('admin_lieu_index') }}" class="btn btn-info btn-sm">Revenir à la liste des lieux</a>

            {{ include('admin/lieu/_delete_form.html.twig') }}
        </div>

    </div>
{% endblock %}
