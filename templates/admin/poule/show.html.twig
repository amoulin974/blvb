{% extends 'base.html.twig' %}

{% block title %}Poule{% endblock %}

{% block body %}
    <h1>Poule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ poule.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ poule.nom }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('admin_poule_index') }}">back to list</a>

    <a href="{{ path('admin_poule_edit', {'id': poule.id}) }}">edit</a>

    {{ include('admin/poule/_delete_form.html.twig') }}
{% endblock %}
