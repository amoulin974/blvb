{% extends 'base.html.twig' %}

{% block title %}Modification de l'utilisateur {{user.email}}{% endblock %}

{% block body %}
    <h1>Modification de l'utilisateur {{user.email}}</h1>

    {{ include('admin/user/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="flex">
        <a href="{{ path('admin_user_index') }}" class="btn btn-info btn-sm">Revenir à la liste des utilisateurst</a>

        {{ include('admin/user/_delete_form.html.twig') }}
    </div>
{% endblock %}
